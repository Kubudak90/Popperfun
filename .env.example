# Popper.fun local / mock fallback. No secrets required to run the UI.
# Copy to apps/web/.env.local if you later wire a real Arc RPC.

NEXT_PUBLIC_ARC_RPC_URL=
NEXT_PUBLIC_FACTORY_ADDRESS=
NEXT_PUBLIC_WALLET_STUB=true
